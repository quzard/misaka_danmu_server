# 这是一个示例环境配置文件。
# 请复制此文件为 .env，并根据您的实际需求修改其中的值。
# .env 文件通常不应提交到版本控制中。

# --- Docker 构建与运行配置 ---
# --- Docker 运行配置 ---
# 设置运行容器的用户和组ID，以匹配您宿主机的用户，避免挂载卷的权限问题。
# 在Linux/macOS上，使用 `id -u` 和 `id -g` 命令获取。
PUID=1000
PGID=1000
UMASK=0022
TZ=Asia/Shanghai

# --- 数据库凭据 ---
# --- 数据库配置 ---
# 数据库类型: mysql (默认) 或 postgresql
DANMUAPI_DATABASE__TYPE=mysql

# 数据库主机。如果应用和数据库在同一个 docker-compose 网络中，可以使用服务名 (e.g., 'danmu-mysql' or 'danmu-postgres')。
# 如果使用 host network_mode，则使用 '127.0.0.1'。
DANMUAPI_DATABASE__HOST=127.0.0.1

# 数据库端口。MySQL 默认是 3306, PostgreSQL 默认是 5432。
DANMUAPI_DATABASE__PORT=3306

# 数据库名称、用户和密码。
# 这些值应该与您在 docker-compose.mysql.yaml 或 docker-compose.postgres.yaml 中设置的值保持一致。
DANMUAPI_DATABASE__NAME=danmuapi
DANMUAPI_DATABASE__USER=danmuapi
DANMUAPI_DATABASE__PASSWORD=your_strong_user_password

# (仅用于MySQL) MySQL root 用户的密码。
DB_ROOT_PASSWORD=your_strong_root_password

# --- 应用安全配置 ---
# 使用 'openssl rand -base64 32' 命令生成一个强密钥。
DANMUAPI_JWT__SECRET_KEY=change_me_to_a_strong_jwt_secret

# --- 初始管理员配置 (可选) ---
DANMUAPI_ADMIN__INITIAL_USER=admin
#DANMUAPI_ADMIN__INITIAL_PASSWORD=a_pre-defined_password # 如果不设置，将生成随机密码